{% extends 'base.html.twig' %}
{% block body %}
<div class="container"style="width:100%;padding-right: 0px;padding-left: 0px">
        <div class="list-group">

            <h5>{{ user.City}}</h5>
            {#<h3>Member:</h3>#}
                    {#<h4 class="list-group-item-heading"> {{ user.username}}</h4>#}
            <div class="container" style="text-align:center;align-content: center;max-width: 1000px;">
                {#<div class="col-xs-6 col-md-3">#}
                <a href="#">
                    <img src="/img/profile/{{ user.id}}.jpg" class="img-circle">
                </a>
                {#</div>#}
            </div>
            <div class="page-header" style="text-align: center;background: white;padding-bottom:0px;padding-top: 0px;">
                <h1 style="color: color;"> {{ user.username}} {{ user.username}} </h1>
            </div>


            <div  class="btn-group" role="group" aria-label="..." style="padding-left: 400px">
                <a href="{{ path('new_message', {'idProfile': user.id}) }}" class="btn btn-default">Contacter</a>
                <a href="{{ path('new_lesson', {'idTeacher': user.id}) }}" class="btn btn-default"> Proposer une course</a>
            </div>
                {#<section class="bg-base-800 quotes" style="background:#373942;">#}
                    <h4 class="list-group-item-text" style="text-align: center;black: white;padding-left: 200px;padding-right: 200px">
                        {{ user.description}}
                    </h4>
                {#</section>#}

            <div class="list-group-item-text" style="padding-left:200px ">
                <h3>Mots clès:
                {% for skill in user.teacherSkills %}
                    <span class="label label-info"> {{ skill.course }}</span>
                {% endfor %}
                </h3>
            </div>

        </div>
</div>
{% endblock %}







